<%= form_for [@world, @concept] do |f| %>
	<div>
		<%= f.label :name %>:
		<%= f.text_field :name %>
	</div>

	<div>
		<%= f.label :description %>
		<%= f.text_area :description %>
	</div>

	<%= f.submit %>
<% end %>
