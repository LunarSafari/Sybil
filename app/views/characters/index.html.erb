<ul>
	<% @characters.each do |c| %>
		<li>
			<%= link_to c.name, character_path(c) %>
			<%= c.brief %>
			<%= link_to 'perspectives', character_perspectives_path(c) %>
		</li>
	<% end %>
</ul>

<%= link_to 'New', new_character_path %>