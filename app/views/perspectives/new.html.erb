<%= form_for @perspective do |f| %>
	<div>
		<%= f.label :content %>
		<%= f.text_area :content %>
	</div>
	<%= hidden_field_tag :character_id, @character.id %>
	<%= hidden_field_tag :concept_id, @concept.id %>
	<%= f.submit %>
<% end %>